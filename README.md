# One-Assignment-of-Python-File-I-O
The game() function in a program lets a user play a game and returns the score as an integer. We need to read a file ‘Hi-score.txt’ which is either blank or contains the previous Hi-score. We need to write a program to update the Hiscore whenever the game() function breaks the Hi-score.
